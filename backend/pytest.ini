[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
testpaths = hos/tests
# Engine tests (hos/tests/test_engine.py) are stdlib unittest and also run
# dependency-free via:  python3 -m unittest hos.tests.test_engine
